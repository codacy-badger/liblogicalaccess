name: Test Github Actions
on:
  push:
    branches:
      - develop
  workflow_dispatch:
jobs:
  Test1:
    runs-on: ubuntu-lates
    steps:
      - run: echo "Test"
      - name: "Checkout"
        uses: actions/checkout@v2
      - name: "List files"
        run: |
          ls ${{ github.workspace }}
      - run: echo "{{ job.status }}"
